# Dependencies for SDN Project with POX Controller and Mininet

# POX Controller dependencies
# (POX will be cloned separately as it's not available on PyPI)

# Mininet dependencies
# (Mininet will be installed separately using system installation)

# Python networking libraries
netifaces>=0.11.0
eventlet>=0.31.0
netaddr>=0.8.0

# General Python utilities
six>=1.16.0
setuptools>=57.0.0

# JSON handling (usually built-in, but ensuring compatibility)
# Testing and development tools (optional)
pyflakes>=2.4.0
pylint>=2.12.0
pep8>=1.7.1

# For potential GUI applications
tk>=0.1.0

# System monitoring
psutil>=5.8.0
